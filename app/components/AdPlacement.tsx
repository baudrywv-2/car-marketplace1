"use client";

/** Placeholder hidden until real ads are ready — keeps browse flow premium. */
export default function AdPlacement(_props: { className?: string; width?: "full" | "sidebar" }) {
  return null;
}
